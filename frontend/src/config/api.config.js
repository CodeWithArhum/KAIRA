export const API_CONFIG = {
    BASE_URL: import.meta.env.VITE_BACKEND_URL,
    ENDPOINTS: {
        CHAT: '/chat',
        STATUS: '/status',
    },
};

export const APP_CONFIG = {
    NAME: 'KAIRA',
    VERSION: '1.0.0',
};
